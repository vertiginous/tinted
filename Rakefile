# -*- ruby -*-

require 'rubygems'
require 'hoe'
require 'metric_fu'

Hoe.spec 'tinted' do
  # self.rubyforge_name = 'tintedx' # if different than 'tinted'
  developer('Gordon Thiesfeld', 'gthiesfeld@gmail.com')
end

# vim: syntax=ruby
